. Boolean operator statements .
===============================

for each (n) from 1 to 100
	if [((n) is divisible by 4) and ((n) is divisible by 5)]
		showline {(n), " is divisible by 20"}

list : filter (1 to 1000) by (n) where
	all of {
		[(n) is divisible by 7],
		[(n) is divisible by 5],
		[(n) is divisible by 3]
	}

showline (list) as list